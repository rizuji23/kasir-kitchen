generator client {
  provider = "prisma-client-js"
  binaryTargets = ["native"]
}

// ======== DATABASE CONFIGURATION ========
datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

